// Arquivo de testes unitários para o UsersController, que define um bloco de testes para o UsersController
describe("UsersController", () => {
  // Cria um caso de teste chamado "test"
  it("test", () => {
    // Exibe uma mensagem no console para indicar que o teste foi executado
    console.log("Passou por aqui");
  });
});
